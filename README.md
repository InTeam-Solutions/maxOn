# maxOn - AI-коуч и планировщик намерений

maxOn — AI-коуч и планировщик намерений с умной декомпозицией целей, прогрессом и календарём для национального мессенджера MAX

Telegram-бот на базе микросервисной архитектуры с контекстно-осведомленным AI агентом.

## 🎯 Возможности

- ✅ **События** - создание, поиск, редактирование событий в календаре
- ✅ **Цели** - диалоговая постановка целей с разбивкой на микрошаги
- ✅ **SMART анализ целей** - автоматический анализ и улучшение формулировок целей
- ✅ **Товары** - поиск и добавление товаров, привязанных к целям
- ✅ **Контекстный AI** - агент помнит историю диалога, активные цели и ближайшие события
- ✅ **Многошаговые диалоги** - state machine для сложных сценариев
- ✅ **Уведомления и напоминания** - гибкая система напоминаний о событиях, дедлайнах и шагах
- ✅ **Мотивационные сообщения** - ежедневные персонализированные сообщения
- ✅ **Ручное управление** - редактирование всех сущностей через удобный UI

## 🏗️ Архитектура

```
┌─────────────────┐
│  Telegram Bot   │
│  (API Gateway)  │
└────────┬────────┘
         │
    ┌────▼─────────┐
    │ Orchestrator │ (координация)
    └─┬──┬────┬────┘
      │  │    │
  ┌───▼──▼────▼───┐
  │ Context │ LLM │ Core │ Worker │
  │ Service │ Srv │ Srv  │ Srv    │
  └─────────┴─────┴──────┴────────┘
          │
    ┌─────▼──────┐
    │ PostgreSQL │
    │   Redis    │
    └────────────┘
```

### Сервисы

1. **Core Service** (:8004) - Business logic (Events, Goals, Products, Cart, Users)
2. **Context Service** (:8002) - Управление памятью, история диалогов, сессии
3. **LLM Service** (:8003) - OpenAI интеграция с контекстными промптами
4. **Orchestrator** (:8001) - Координация сервисов и state machine
5. **API Gateway** - Telegram bot с rich HTML рендерингом
6. **Worker** (:8005) - Фоновые задачи и система уведомлений

## 🚀 Быстрый старт

### Требования

- Docker & Docker Compose

### 1. Клонировать репозиторий

```bash
git clone https://github.com/InTeam-Solutions/maxOn.git
cd maxOn
```

### 2. Настроить переменные окружения

Создайте `.env` файл (не коммитить!):

```bash
# ОБЯЗАТЕЛЬНО замените эти значения:
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
OPENAI_API_KEY=your_openai_api_key_here
POSTGRES_PASSWORD=secure_password_here
RABBITMQ_PASSWORD=secure_password_here
```

Как получить токены:
- **Telegram**: @BotFather → /newbot
- **OpenAI**: https://platform.openai.com/api-keys

### 3. Запустить все сервисы

```bash
docker compose up -d
```

### 4. Проверить статус

```bash
docker compose ps
```

Должны работать:
- ✅ postgres (5432)
- ✅ redis (6379)
- ✅ core (8004)
- ✅ context (8002)
- ✅ llm (8003)
- ✅ orchestrator (8001)
- ✅ worker (8005)
- ✅ api-gateway (Telegram bot)

### 5. Просмотреть логи

```bash
# Все сервисы
docker compose logs -f

# Конкретный сервис
docker compose logs -f api-gateway
```

### 6. API документация

После запуска доступна Swagger UI:

- Core Service: http://localhost:8004/docs
- Context Service: http://localhost:8002/docs
- LLM Service: http://localhost:8003/docs
- Worker Service: http://localhost:8005/docs

## 📱 Использование бота

### Основные команды

- `/start` - Запуск бота и главное меню
- `/goals` - Показать все цели
- `/events` - Показать календарь событий
- `/settings` - Настройки уведомлений

### Создание целей

Просто напиши боту свою цель, например:
- "Хочу выучить английский до уровня B2 за 6 месяцев"
- "Похудеть на 10 кг за 3 месяца"
- "Научиться программировать на Python"

Бот автоматически:
1. Проанализирует цель по SMART-критериям
2. Предложит улучшения
3. Создаст пошаговый план достижения
4. Запланирует шаги в календаре

### Система уведомлений

В `/settings` можно настроить:
- 🔔 Напоминания о событиях (за N минут до начала)
- 📅 Предупреждения о дедлайнах целей (за 7, 3, 1 день)
- 📋 Напоминания о незавершенных шагах (ежедневно в 20:00)
- 💪 Мотивационные сообщения (ежедневно в 8:00)

## 📋 Основные команды

```bash
docker compose up -d              # Запустить все сервисы
docker compose down               # Остановить все сервисы
docker compose ps                 # Статус контейнеров
docker compose logs -f            # Просмотр логов всех сервисов
docker compose logs -f worker     # Логи конкретного сервиса
docker compose restart core       # Перезапустить сервис
docker compose build              # Пересобрать образы
docker compose down -v            # Удалить все (⚠️ потеря данных!)
```

### Подключение к базам данных

```bash
# PostgreSQL
docker compose exec postgres psql -U initio -d initio

# Redis
docker compose exec redis redis-cli
```

## 🗄️ База данных

### Схема

**Core Service:**
- `events` - события календаря (с полями reminder_minutes_before, reminder_enabled)
- `goals` - цели пользователя
- `steps` - микрошаги целей
- `products` - товары
- `cart_items` - корзина
- `users` - пользователи с настройками уведомлений

**Context Service:**
- `user_profiles` - профили пользователей
- `conversation_messages` - история диалогов
- `session_states` - состояния сессий

## 🔔 Система уведомлений

Worker Service с APScheduler выполняет 4 типа задач:

1. **Event Reminders** (каждую минуту)
   - Проверяет события, до которых осталось N минут
   - Отправляет напоминания с деталями события

2. **Goal Deadline Warnings** (ежедневно в 9:00)
   - Предупреждает за 7, 3, 1 день до дедлайна
   - Показывает прогресс выполнения

3. **Unfinished Step Reminders** (ежедневно в 20:00)
   - Напоминает о просроченных шагах
   - Группирует по целям

4. **Motivational Messages** (ежедневно в 8:00)
   - Персонализированные мотивационные сообщения
   - Показывает текущие цели и прогресс

## 🧪 Тестирование

### Проверка Worker

```bash
# Список активных задач
curl http://localhost:8005/jobs | python3 -m json.tool

# Логи worker
docker compose logs -f worker
```

### Ручное тестирование API

**Создать событие:**
```bash
curl -X POST http://localhost:8004/api/events \
  -H "Content-Type: application/json" \
  -d '{
    "user_id": "123",
    "title": "Встреча",
    "date": "2025-10-01",
    "time": "15:00",
    "reminder_minutes_before": 15,
    "reminder_enabled": true
  }'
```

**Получить настройки пользователя:**
```bash
curl http://localhost:8004/api/users/123
```

**Парсинг сообщения через LLM:**
```bash
curl -X POST http://localhost:8003/api/parse \
  -H "Content-Type: application/json" \
  -d '{
    "message": "Покажи мои события на завтра",
    "context": {
      "profile": {"timezone": "Europe/Moscow"},
      "active_goals": [],
      "upcoming_events": []
    }
  }'
```

## 📊 Мониторинг

### Health Checks

```bash
curl http://localhost:8004/health  # Core
curl http://localhost:8002/health  # Context
curl http://localhost:8003/health  # LLM
curl http://localhost:8005/health  # Worker
```

## 🔧 Разработка

### Добавление нового поля в Event

1. Обновить модель: `services/core/app/models/event.py`
2. Обновить схему: `shared/schemas/events.py`
3. Перезапустить сервис: `docker compose restart core`

### Изменение промптов

Промпты в Jinja2 шаблонах:
- `services/llm/app/prompts/system.py` - основной system prompt
- `services/llm/app/prompts/summarizer.py` - суммаризация ответов
- `services/llm/app/prompts/goal_coach.py` - генерация шагов целей

После изменения перезапустите сервис:
```bash
docker compose restart llm
```

### Добавление нового типа уведомлений

1. Создать task в `services/worker/app/tasks/`
2. Зарегистрировать в `services/worker/app/scheduler.py`
3. Пересобрать: `docker compose build worker && docker compose up -d worker`

### Очистка кеша LLM

```bash
curl -X DELETE http://localhost:8003/api/cache
```

## 🐛 Troubleshooting

### Ошибка "POSTGRES_PASSWORD is required"

Проверьте `.env` файл - должна быть установлена переменная `POSTGRES_PASSWORD`.

### Ошибка "OpenAI API key not found"

Установите `OPENAI_API_KEY` в `.env` файле.

### Контейнер не запускается

```bash
# Проверить логи
docker compose logs <service_name>

# Пересобрать контейнер
docker compose build <service_name>
docker compose up -d <service_name>
```

### База данных не создается

```bash
# Пересоздать volumes
docker compose down -v
docker compose up -d
```

### Worker не отправляет уведомления

```bash
# Проверить задачи
curl http://localhost:8005/jobs

# Проверить логи
docker compose logs worker

# Убедиться что у пользователя есть chat_id
curl http://localhost:8004/api/users/<user_id>
```

## 📚 Документация

См. `CLAUDE.md` для детальной архитектуры и руководства по разработке.

## ✅ Реализовано

- ✅ Микросервисная архитектура
- ✅ Telegram bot с rich UI
- ✅ SMART анализ целей
- ✅ Автогенерация шагов для целей
- ✅ Календарь с группировкой по датам
- ✅ Ручное управление всеми сущностями
- ✅ Система уведомлений и напоминаний
- ✅ Настройки пользователя
- ✅ Worker Service с APScheduler

## 🚧 Roadmap

- [ ] Product Search - интеграция с маркетплейсами
- [ ] Recurring Events - автогенерация повторяющихся событий
- [ ] Integration Tests
- [ ] Адаптация для MAX мессенджера
- [ ] Deploy на продакшен (Kubernetes)

## 📝 Лицензия

MIT

## 👨‍💻 Разработка

Создано с помощью Claude Code.

---

**Нужна помощь?** Проверьте логи: `docker compose logs -f`
